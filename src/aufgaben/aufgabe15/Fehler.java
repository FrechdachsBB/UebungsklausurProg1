/*package aufgaben.aufgabe15;

public class Fehler {
    private int[] b //Semikolon fehlt
    public Fehler(int b) {
        this.b = b; //array wird ein einzelner int zugewiesen. Signatur ändern zu public Fehler(int[] b){
    }

    public int operation1(int c) {
        if (c < 0 || c >= b.length()) //runde Klammern nach length entfernen
            return -1;
        return b(c); //runde Klammern durch eckige Klammern ersetzen
    }

    public boolean operation2(int c) //öffnende geschweifte Klammer fehlt
        for (int c = 0; i < b.length; i++) //int c ersetzen durch int i
            if (n[i] == c) //n ersetzen durch b
                return true;
        //default return value fehlt
        //Zeile return false; einfügen
    }
}*/
